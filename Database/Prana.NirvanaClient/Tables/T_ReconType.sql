CREATE TABLE [dbo].[T_ReconType] (
    [ReconTypeID]   INT           NOT NULL,
    [ReconTypeName] VARCHAR (MAX) NOT NULL,
    CONSTRAINT [PK_T_ReconType] PRIMARY KEY CLUSTERED ([ReconTypeID] ASC)
);

