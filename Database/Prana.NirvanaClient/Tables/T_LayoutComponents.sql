CREATE TABLE [dbo].[T_LayoutComponents] (
    [ComponentID]   INT          NOT NULL,
    [ComponentName] VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_T_LayoutComponents] PRIMARY KEY CLUSTERED ([ComponentID] ASC)
);

